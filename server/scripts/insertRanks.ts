// import { insertRankings } from '../db';
// import { readFile } from 'fs/promises';

// async function insertRankingsFromFile(filePath) {
//   const data = await readFile(filePath, 'utf-8');
//   const rankings = JSON.parse(data);
//   // Now you have a JavaScript array and can pass it to your insertRankings function
//   await insertRankings('Your Division Name', rankings);
// }
// insertRankingsFromFile('C:/Users/omidd/OneDrive/Desktop/poundforPound.json')
//   .then(() => console.log('Rankings have been inserted.'))
//   .catch((error: Error) => console.error('Error inserting rankings:', error));
