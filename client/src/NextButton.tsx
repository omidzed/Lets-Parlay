import { RxChevronRight } from 'react-icons/rx';

// type NextButtonProps = {
//   index: number;
//   onNextClick: (index: number) => void;
// };

export function NextButton() {
  return <RxChevronRight />;
}
// { index, onNextClick }: NextButtonProps)
